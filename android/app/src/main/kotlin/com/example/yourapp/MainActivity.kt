package com.example.yourapp

import io.flutter.embedding.android.FlutterActivity

class MainActivity: FlutterActivity() {
    // This is the main entry point for the Flutter application.
    // You can override methods here if you need to customize the behavior.

    // Example: Override onCreate method if you need to perform actions when the activity is created
    /*
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // Custom initialization code can go here
    }
    */

    // Example: If you want to handle specific intents or actions
    /*
    override fun onNewIntent(intent: Intent?) {
        super.onNewIntent(intent)
        // Handle new intents here
    }
    */
} 